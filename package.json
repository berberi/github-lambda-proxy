{
  "name": "github-lambda-proxy",
  "version": "1.0.0",
  "files": [
    "bin"
  ],
  "description": "",
  "main": "index.js",
  "scripts": {
    "transpile": "babel src --out-dir bin --presets=@babel/preset-env",
    "create": "npm run transpile && claudia create --region us-east-2 --api-module bin/api",
    "update": "npm run transpile && claudia update"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@octokit/rest": "^16.28.7",
    "claudia-api-builder": "^4.1.2"
  },
  "devDependencies": {
    "@babel/cli": "^7.5.5",
    "@babel/core": "^7.5.5",
    "@babel/preset-env": "^7.5.5",
    "claudia": "^5.7.0"
  }
}
